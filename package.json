{
  "name": "my-v0-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate-images": "node scripts/convert-images.js",
    "build": "npm run build:site || echo \"define your build script\""
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
